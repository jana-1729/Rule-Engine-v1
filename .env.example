# =============================================================================
# DATABASE
# =============================================================================

# PostgreSQL connection string
# Local: postgresql://postgres:password@localhost:5432/rule_engine
# Supabase: Get from Supabase project settings → Database → Connection string
DATABASE_URL="postgresql://postgres:password@localhost:5432/rule_engine"

# =============================================================================
# REDIS / QUEUE
# =============================================================================

# Redis connection for job queues
# Local: redis://localhost:6379
# Upstash: Get from Upstash console
REDIS_URL="redis://localhost:6379"

# =============================================================================
# ENCRYPTION & SECURITY
# =============================================================================

# Encryption key for storing OAuth tokens (32 bytes)
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY="your-32-byte-encryption-key-change-this"

# Session secret for cookie signing
# Generate with: openssl rand -base64 32
SESSION_SECRET="your-session-secret-key-change-this"

# =============================================================================
# SLACK INTEGRATION
# =============================================================================

# Get these from: https://api.slack.com/apps
# 1. Create new app at https://api.slack.com/apps
# 2. Go to "OAuth & Permissions" → Add redirect URL
# 3. Add scopes: chat:write, channels:read, users:read, team:read
# 4. Get credentials from "Basic Information" → "App Credentials"

SLACK_CLIENT_ID="your-slack-client-id"
SLACK_CLIENT_SECRET="your-slack-client-secret"
SLACK_REDIRECT_URI="http://localhost:3000/api/v1/connections/callback"

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

NODE_ENV="development"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# =============================================================================
# OPTIONAL: LOGGING & MONITORING
# =============================================================================

LOG_LEVEL="info"

# Sentry for error tracking (optional)
# SENTRY_DSN="https://your-sentry-dsn@sentry.io/project-id"

# =============================================================================
# OPTIONAL: RATE LIMITING
# =============================================================================

RATE_LIMIT_MAX_REQUESTS="100"
RATE_LIMIT_WINDOW_MS="60000"

# =============================================================================
# NOTES
# =============================================================================

# 1. Copy this file: cp .env.example .env
# 2. Generate secure keys: openssl rand -base64 32
# 3. Update DATABASE_URL and REDIS_URL with your values
# 4. Add Slack credentials when ready to test OAuth
# 5. Never commit .env to git
